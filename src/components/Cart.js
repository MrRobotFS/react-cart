import React, { Fragment } from 'react';

const Cart = ({product}) => {
    
    const {key,name,price} = product;

    return (
    <Fragment>
        <ul>
            <li></li>
        </ul>
    </Fragment>
    );
}

export default Cart;